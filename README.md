# Docker Build

This is project that can be runned using docker. The aim of the project is to write a docker file that is able to run the full stack mern application on a single server.

## How to run the project

```console
1. docker build -t test .
2. docker run -p 8000:8000 test
```

OR

```console
docker-compose up --build
```
